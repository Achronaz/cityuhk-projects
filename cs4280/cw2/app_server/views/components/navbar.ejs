<nav class="navbar navbar-expand-lg navbar-light sticky-top" style="position: sticky;">
    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01"
            aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="/">
            Music Store
        </a>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
            <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                <% if(user !== undefined) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/history">History</a>
                    </li>
                    <% if(user.permission === 1) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/manage">Manage</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/statistic">Statistic</a>
                        </li>
                    <% } %>
                <% } %>
            </ul>
            <form class="form-inline mr-sm-2">
                <div class="input-group">
                    <select class="form-control" id="search_option">
                        <option value="song" selected>Song</option>
                        <option value="album">Album</option>
                        <option value="artist">Artist</option>
                    </select>
                    <div class="input-group-append"></div>
                    <div class="input-group-prepend"></div>
                    <input class="form-control" type="search" placeholder="search ..." id="search_key">
                    <div class="input-group-append"></div>
                    <div class="input-group-prepend"></div>
                    <button class="form-control" type="button" value="Search" id="search_button"><i class="fas fa-search"></i></button>
                    <!--<input class="form-control" type="button" value="Search" >-->
                </div>
            </form>

            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <% if (user !== undefined) { %>
                    <a href="#" class="nav-link dropdown-toggle" id="navDropDownLink" data-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false">Profile</a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navDropDownLink"
                        style="padding: 8px;">
                        <p><%=user.username %></p>
                        <p>Points: <%=user.loyaltypoints %></p>
                        <div class="dropdown-divider"></div>
                        <button type="button" class="form-control" onclick="signout()">Signout</button>
                    </div>
                    <% } else { %>
                    <a href="#" class="nav-link" data-toggle="modal"
                        data-target="#modal_sign">Signin</a>
                    <% } %>
                </li>
            </ul>
        </div>
    </div>

</nav>